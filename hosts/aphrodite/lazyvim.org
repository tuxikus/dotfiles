#+title: Lazyvim
#+author: tuxikus
#+property: header-args :mkdirp yes

* init.lua

#+begin_src lua :tangle ~/.config/lazyvim/init.lua
require("config.lazy")
#+end_src

* Config

** Lazy

#+begin_src lua :tangle ~/.config/lazyvim/lua/config/lazy.lua
local lazypath = vim.fn.stdpath("data") .. "/lazy/lazy.nvim"
if not (vim.uv or vim.loop).fs_stat(lazypath) then
  local lazyrepo = "https://github.com/folke/lazy.nvim.git"
  local out = vim.fn.system({ "git", "clone", "--filter=blob:none", "--branch=stable", lazyrepo, lazypath })
  if vim.v.shell_error ~= 0 then
    vim.api.nvim_echo({
      { "Failed to clone lazy.nvim:\n", "ErrorMsg" },
      { out, "WarningMsg" },
      { "\nPress any key to exit..." },
    }, true, {})
    vim.fn.getchar()
    os.exit(1)
  end
end
vim.opt.rtp:prepend(lazypath)

require("lazy").setup({
  spec = {
    -- add LazyVim and import its plugins
    { "LazyVim/LazyVim", import = "lazyvim.plugins" },
    -- import/override with your plugins
    { import = "plugins" },
  },
  defaults = {
    -- By default, only LazyVim plugins will be lazy-loaded. Your custom plugins will load during startup.
    -- If you know what you're doing, you can set this to `true` to have all your custom plugins lazy-loaded by default.
    lazy = false,
    -- It's recommended to leave version=false for now, since a lot the plugin that support versioning,
    -- have outdated releases, which may break your Neovim install.
    version = false, -- always use the latest git commit
    -- version = "*", -- try installing the latest stable version for plugins that support semver
  },
  install = { colorscheme = { "tokyonight", "habamax" } },
  checker = {
    enabled = true, -- check for plugin updates periodically
    notify = false, -- notify on update
  }, -- automatically check for plugin updates
  performance = {
    rtp = {
      -- disable some rtp plugins
      disabled_plugins = {
        "gzip",
        -- "matchit",
        -- "matchparen",
        -- "netrwPlugin",
        "tarPlugin",
        "tohtml",
        "tutor",
        "zipPlugin",
      },
    },
  },
})
#+end_src

** Keymaps

#+begin_src lua :tangle ~/.config/lazyvim/lua/config/keymaps.lua
vim.keymap.set({ "n" }, "<leader>fs", "<cmd>:w<cr>", { desc = "File save" })
#+end_src


** Options

#+begin_src lua :tangle ~/.config/lazyvim/lua/config/options.lua
vim.g.snacks_animate = false
#+end_src

** Auto commands

#+begin_src lua :tangle ~/.config/lazyvim/lua/config/autocmds.lua
#+end_src

* Plugins

** Colorscheme

#+begin_src lua :tangle ~/.config/lazyvim/lua/plugins/colorscheme.lua
return {
  {
    "LazyVim/LazyVim",
    opts = {
      colorscheme = "catppuccin-latte",
    },
  }
}
#+end_src

** Yazi

#+begin_src lua :tangle ~/.config/lazyvim/lua/plugins/yazi.lua
return {
  "mikavilpas/yazi.nvim",
  version = "*",
  event = "VeryLazy",
  dependencies = {
    { "nvim-lua/plenary.nvim", lazy = true },
  },
  keys = {
    { "<leader>fy", mode = { "n", "v" }, "<cmd>Yazi<cr>", desc = "Yazi at the current file" },
    { "<leader>fY", "<cmd>Yazi cwd<cr>", desc = "Yazi at vim root" },
    -- { "<c-up>", "<cmd>Yazi toggle<cr>", desc = "Resume the last yazi session" },
  },
  opts = {
    open_for_directories = true,
    keymaps = {
      show_help = "<f1>",
    },
  },
  init = function()
    vim.g.loaded_netrwPlugin = 1
  end,
}
#+end_src

** lsp

#+begin_src lua :tangle ~/.config/lazyvim/lua/plugins/lsp.lua
return {
  "neovim/nvim-lspconfig",
  opts = {
    inlay_hints = { enabled = false },
  },
}
#+end_src

** Snacks

#+begin_src lua :tangle ~/.config/lazyvim/lua/plugins/snacks.lua
return {
  "folke/snacks.nvim",
  opts = {
    dashboard = { enabled = false },
  },
}
#+end_src


** Disabled

#+begin_src lua :tangle ~/.config/lazyvim/lua/plugins/disabled.lua
return {
  { "akinsho/bufferline.nvim", enabled = false },
  { "nvim-lualine/lualine.nvim", enabled = false },
}
#+end_src

** Slimline

#+begin_src lua :tangle ~/.config/lazyvim/lua/plugins/slimline.lua
return {
  "sschleemilch/slimline.nvim",
  opts = {
    style = "fg",
    hl = {
      primary = "StatusLine",
      secondary = "StatusLineNC",
      base = "StatusLine",
      base_inactive = "StatusLineNC",
    },
    configs = {
      progress = {
        follow = false,
      },
    },
    spaces = {
      components = "",
      left = "",
      right = "",
    },
  },
}
#+end_src

** Compile Mode

#+begin_src #+begin_src lua :tangle ~/.config/lazyvim/lua/plugins/compile-mode.lua
return {
  "ej-shafran/compile-mode.nvim",
  version = "^5.0.0",
  -- you can just use the latest version:
  -- branch = "latest",
  -- or the most up-to-date updates:
  -- branch = "nightly",
  dependencies = {
    "nvim-lua/plenary.nvim",
    -- if you want to enable coloring of ANSI escape codes in
    -- compilation output, add:
    -- { "m00qek/baleia.nvim", tag = "v1.3.0" },
  },
  keys = {
    { "<leader>c<leader>", mode = { "n" }, "<cmd>Compile<cr>", desc = "Compile" },
    { "<leader>cn", mode = { "n" }, "<cmd>NextError<cr>", desc = "Compile next error" },
    { "<leader>cp", mode = { "n" }, "<cmd>PrevError<cr>", desc = "Compile prev error" },
  },
  config = function()
    ---@type CompileModeOpts
    vim.g.compile_mode = {
      -- if you use something like `nvim-cmp` or `blink.cmp` for completion,
      -- set this to fix tab completion in command mode:
      -- input_word_completion = true,

      -- to add ANSI escape code support, add:
      -- baleia_setup = true,

      -- to make `:Compile` replace special characters (e.g. `%`) in
      -- the command (and behave more like `:!`), add:
      -- bang_expansion = true,
    }
  end,
}
#+end_src
